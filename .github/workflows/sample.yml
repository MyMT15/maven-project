name: sample-pipeline
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:
jobs:
  job1:
    runs_on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: running a shell commad
        run: echo "this is my 1st CICD with Github actions"
  job2:
    runs_on:
    steps:
      - uses: actions/checkout@v2
      - name: building the docker image
        run: docker build . --file Dockerfile --tag dock1
